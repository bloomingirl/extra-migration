apiVersion: v2
name: eks-monitoring
description: Prometheus + Grafana + Alertmanager stack for extra-migration EKS cluster
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: kube-prometheus-stack
    version: "61.7.2"
    repository: "https://prometheus-community.github.io/helm-charts"
    condition: kube-prometheus-stack.enabled
